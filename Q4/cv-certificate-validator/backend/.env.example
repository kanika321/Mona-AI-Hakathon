# Get a key from https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Using a different port than the work-permit-validator project (4000)
# so both can run side by side if needed.
PORT=4001
